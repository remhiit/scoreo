// See `./player`: one schema for the whole workspace, so both projects read and
// write the same bytes under `remhiit.github.io`'s shared localStorage.
export { PlayerSchema } from '@scoreboards/shared-domain'
